{
  "name": "ticket-to-ride",
  "version": "1.0.0",
  "description": "Квиток на поїзд - онлайн настільна гра",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "npm install --prefix client && npm run build --prefix client",
    "dev": "concurrently \"node server/index.js\" \"cd client && npx vite --host\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4",
    "uuid": "^9.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "20.x"
  }
}
